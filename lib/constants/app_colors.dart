import 'package:flutter/material.dart';

class AppColors {
  //  dark background
  static const Color background = Color(0xFF0A0027);

  // Primary Purple color for buttons and highlights
  static const Color primaryPurple = Color(0xFF0732D3);

  // Text colors
  static const Color white = Colors.white;
  static Color lightWhite = Colors.white;
  static const Color indicatorDot = Colors.white54;

  // Gradient Colors
  static const Color gradientStart = Color(0xff100137);
  static const Color gradientEnd = Color(0xFF022893);

  // Design-er shonge mil rakhar janyo ekta List toiri kora
  static const List<Color> backgroundGradient = [
    gradientStart,
    gradientEnd,
  ];
}